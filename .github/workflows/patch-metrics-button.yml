name: Apply backend safeguards

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/patch-metrics-button.yml
      - .github/scripts/patch_metrics_button.py
      - .github/scripts/prevent_duplicate_submissions.py

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Apply metrics patch
        run: python .github/scripts/patch_metrics_button.py

      - name: Apply duplicate submission safeguard
        run: python .github/scripts/prevent_duplicate_submissions.py

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add server/src/index.js src/pages/admin/SubmissionsAdmin.tsx
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Prevent duplicate posts in the same campaign"
            git push
          fi
